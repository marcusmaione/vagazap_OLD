<div class="container col-lg-4 col-lg-offset-4 col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
  <div class="profile-box">

  <!-- If user.degrees.nil? ou user.experiences.nil? -->
    <% if @user.degrees.empty? || @user.experiences.empty? %>

  <!-- call to action (botao) para completar perfil  -->
      <p>Complete seu perfil. Perfis completos têm mais chances de serem escolhidos pelas empresas.</p>
      <%= link_to "Completar perfil", root_path, class: "btn btn-primary" %>
      <!-- mudar path do botao para new degree, que quando terminar vai pra new experience e depois new course -->

  <!-- else -->
    <% else %>

  <!-- call to action para ver as vagas proximas (botao vermelho)-->
      <%= link_to "Começar", root_path, class: "btn btn-primary" %>

  <!-- end -->
    <% end %>

  <!-- lista o perfil todo rendering cada item como no profile page do deplastify-->

  <!-- link para editar perfil -->


  </div>
</div>
