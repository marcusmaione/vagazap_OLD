<div class="container col-lg-4 col-lg-offset-4 col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
  <div class="profile-box">

    <div class="degrees-box">
      <div class="degree-box">

        <p><%= @company.name %></p>
        <p>Setor: <%= @company.sector %></p>
        <p>Endereço: <%= @company.address %></p>
        <p><i class="fas fa-phone"></i> <%= @company.phone %></p>

      </div>
    </div>

    <%= link_to "Criar vaga", new_company_job_path(@company), class: "btn btn-primary" %>

    <br>

    <% @company.jobs.each do |job| %>
      <% job.title %>
    <% end %>



    <%= link_to "Voltar", profile_path, class: "btn btn-tertiary btn-sm" %>

  </div>
</div>
