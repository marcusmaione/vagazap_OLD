<div class="container col-lg-4 col-lg-offset-4 col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">

<h5><%= @company.name %></h5>

<div class="degrees-box">
    <div class="degree-box">

      <p>Setor: <%= @company.sector %></p>
      <p>Endereço: <%= @company.address %></p>
      <p><i class="fas fa-phone"></i> <%= @company.phone %></p>
      <p>Crie uma vaga para <%= @company.name %></p>

      <%#= link_to "Criar vaga", new_company_job_path, class: "btn btn-primary" %>

<!--       EACH DO PARA LISTAR TODAS AS VAGAS DESTA EMPRESA
CADA VAGA COMO UM CARD DENTRO DO CARD DA EMPRESA
CADA VAGA COM BOTAO PARA PROCURAR CANDIDATOS, EDITAR E REMOVER
 -->
      <br>

      <% @company.jobs.each do |job| %>
        <% job.title %>
      <% end %>

    </div>

</div>

<%= link_to "Voltar", profile_path, class: "btn btn-secondary" %>

</div>
