<div class="container col-lg-4 col-lg-offset-4 col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
  <div class="profile-box">

    <h5>Candidatos para <%= @job.title %>:</h5>

    <div class="degrees-box">
      <% @all_candidates.each do |candidate| %>

        <% if candidate[1] <= 0 %>
          <div class="degree-box">
            <p>Nome: <%= candidate[0].first_name %> <%= candidate[0].last_name %> </p>
            <p>Experiência como <%= candidate[0].experiences[0].title %>: <%= candidate[0].experiences[0].level %> </p>
            <p>Diploma: <%= candidate[0].degrees[0].level %> </p>
            <div class="complete-profile-box">
              <%= link_to "Perfil completo", job_candidate_path(@job.company_id, @job, candidate[0].id), class: "btn btn-secondary btn-sm" %>
            </div>
          </div>
        <% else %>
        <% end %>

      <% end %>
    </div>

    <br>
    <%= link_to "Voltar", company_jobs_path, class: "btn btn-tertiary btn-sm" %>

  </div>
</div>

